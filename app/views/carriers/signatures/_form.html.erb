<div class="space-y-6">
  <div>
    <nav class="flex text-sm text-slate-300" aria-label="Breadcrumb">
      <ol class="inline-flex items-center space-x-1 md:space-x-3">
        <li class="inline-flex items-center">
          <%= link_to root_path, class: "text-slate-300 hover:text-white" do %>
            <span class="material-icons text-sm mr-1">home</span>
            Home
          <% end %>
        </li>
        <li>
          <div class="flex items-center">
            <span class="material-icons text-sm text-slate-500 mx-1">chevron_right</span>
            <%= link_to "Carriers", carriers_path, class: "text-slate-300 hover:text-white" %>
          </div>
        </li>
        <li>
          <div class="flex items-center">
            <span class="material-icons text-sm text-slate-500 mx-1">chevron_right</span>
            <%= link_to @carrier.name, carrier_path(@carrier), class: "text-slate-300 hover:text-white" %>
          </div>
        </li>
        <li>
          <div class="flex items-center">
            <span class="material-icons text-sm text-slate-500 mx-1">chevron_right</span>
            <span class="text-white font-medium"><%= @signature.persisted? ? "Update Signature" : "Add Signature" %></span>
          </div>
        </li>
      </ol>
    </nav>
  </div>

  <div class="rounded-2xl border border-dashed border-white/20 bg-white/5 p-6 shadow-xl shadow-black/30 backdrop-blur">
    <h1 class="text-xl font-semibold text-white mb-4">
      <%= @signature.persisted? ? "Update Carrier Signature" : "Add Carrier Signature" %>
    </h1>
    <p class="text-slate-300 text-sm mb-6">
      Store a reusable signature payload (base64 PNG, JSON blob, or secure token). The automation service
      reuses this value in every legal form generated for <%= @carrier.name %>.
    </p>

    <%= form_with model: [@carrier, @signature], class: "space-y-4" do |form| %>
      <div>
        <%= form.label :signature, "Signature payload", class: "block text-sm font-medium text-slate-200" %>
        <%= form.text_area :signature, rows: 8, class: "mt-2 block w-full rounded-xl border border-white/10 bg-white/5 p-3 text-white placeholder:text-slate-400 focus:outline-none focus:ring-2 focus:ring-yellow-400/30", placeholder: "data:image/png;base64,iVBORw0KGgo..." %>
        <p class="mt-2 text-xs text-slate-400">
          Paste the exact payload your signing tool outputs. We keep it encrypted at rest and embed it in lawyer packets only.
        </p>
      </div>

      <div class="flex items-center gap-3">
        <%= form.submit(@signature.persisted? ? "Update signature" : "Save signature", class: "inline-flex items-center rounded-xl bg-yellow-400 px-5 py-2 text-sm font-semibold text-black shadow-lg shadow-yellow-500/20 hover:bg-yellow-300") %>
        <%= link_to "Cancel", carrier_path(@carrier), class: "inline-flex items-center rounded-xl border border-white/10 bg-white/5 px-4 py-2 text-sm font-semibold text-white hover:bg-white/10" %>
      </div>
    <% end %>
  </div>
</div>
