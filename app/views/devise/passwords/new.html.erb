<div class="flex min-h-screen items-center justify-center bg-gray-50 px-4 py-12">
  <div class="w-full max-w-md">
    <!-- Logo/Header -->
    <div class="mb-8 text-center">
      <h1 class="text-3xl font-bold text-dark">Rails Fast Epost</h1>
      <p class="mt-2 text-sm text-body">Reset your password</p>
    </div>

    <!-- Password Reset Card -->
    <div class="rounded-sm border border-stroke bg-white px-8 py-10 shadow-default">
      <p class="mb-6 text-sm text-body">Enter your email address and we'll send you instructions to reset your password.</p>

      <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post, class: "space-y-5" }) do |f| %>
        <!-- Error Messages -->
        <% if resource.errors.any? %>
          <div class="rounded-sm border border-danger/30 bg-[#FFF1F2] p-4 text-danger">
            <h3 class="mb-2 font-semibold text-sm"><%= pluralize(resource.errors.count, "error") %> occurred:</h3>
            <ul class="list-disc pl-5 text-xs">
              <% resource.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
              <% end %>
            </ul>
          </div>
        <% end %>

        <!-- Email Field -->
        <div>
          <%= f.label :email, class: "mb-2 block text-sm font-medium text-dark" %>
          <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "w-full rounded border border-stroke bg-white px-4 py-3 text-dark focus:border-primary focus:outline-none" %>
        </div>

        <!-- Submit Button -->
        <div>
          <%= f.submit "Send reset instructions", class: "w-full rounded bg-primary px-4 py-3 text-sm font-medium text-white hover:opacity-90 focus:outline-none" %>
        </div>
      <% end %>

      <!-- Links -->
      <div class="mt-6 text-center text-sm">
        <%= render "devise/shared/links" %>
      </div>
    </div>
  </div>
</div>
