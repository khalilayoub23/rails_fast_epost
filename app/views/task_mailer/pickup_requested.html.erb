<!DOCTYPE html>
<html>
<head>
  <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  <style>
    body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
    .container { max-width: 600px; margin: 0 auto; padding: 20px; }
    .header { background-color: #3b82f6; color: white; padding: 20px; text-align: center; }
    .content { background-color: #f9fafb; padding: 20px; margin: 20px 0; }
    .pickup-details { background-color: white; padding: 20px; border-left: 4px solid #3b82f6; }
    .detail-row { margin: 10px 0; }
    .label { font-weight: bold; color: #6b7280; }
    .value { color: #111827; }
    .button { background-color: #3b82f6; color: white; padding: 12px 24px; text-decoration: none; display: inline-block; margin: 20px 0; border-radius: 4px; }
    .urgent { background-color: #fef3c7; border-left: 4px solid #f59e0b; padding: 15px; margin: 15px 0; }
    .footer { text-align: center; color: #6b7280; font-size: 12px; margin-top: 20px; }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h1>📦 Pickup Request</h1>
    </div>
    
    <div class="content">
      <p>Hello <%= @messenger.name %>,</p>
      <p>A pickup has been requested for the following task:</p>
      
      <div class="pickup-details">
        <div class="detail-row">
          <span class="label">Tracking Number:</span>
          <span class="value"><%= @task.barcode %></span>
        </div>
        <div class="detail-row">
          <span class="label">Sender:</span>
          <span class="value"><%= @sender.name %></span>
        </div>
        <div class="detail-row">
          <span class="label">Sender Phone:</span>
          <span class="value"><%= @sender.phone %></span>
        </div>
        <div class="detail-row">
          <span class="label">Pickup Address:</span>
          <span class="value"><%= @task.pickup_location %></span>
        </div>
        <% if @task.scheduled_pickup_time.present? %>
        <div class="detail-row">
          <span class="label">Scheduled Time:</span>
          <span class="value"><%= @task.scheduled_pickup_time.strftime("%B %d, %Y at %I:%M %p") %></span>
        </div>
        <% end %>
        <div class="detail-row">
          <span class="label">Delivery Address:</span>
          <span class="value"><%= @task.drop_off_location %></span>
        </div>
      </div>
      
      <% if @task.priority == 'urgent' || @task.priority == 'express' %>
      <div class="urgent">
        ⚠️ <strong>URGENT:</strong> This is a <%= @task.priority.titleize %> delivery. Please prioritize this pickup.
      </div>
      <% end %>
      
      <center>
        <a href="<%= root_url %>" class="button">View Task Details</a>
      </center>
      
      <p><strong>Next Steps:</strong></p>
      <ul>
        <li>Contact the sender to confirm pickup time</li>
        <li>Navigate to the pickup location</li>
        <li>Update the task status when you arrive</li>
        <li>Verify the package details before pickup</li>
      </ul>
    </div>
    
    <div class="footer">
      <p>FastePost Delivery System</p>
    </div>
  </div>
</body>
</html>
