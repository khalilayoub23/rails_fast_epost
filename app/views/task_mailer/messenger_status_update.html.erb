<!DOCTYPE html>
<html>
<head>
  <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  <style>
    body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
    .container { max-width: 600px; margin: 0 auto; padding: 20px; }
    .header { background-color: #8b5cf6; color: white; padding: 20px; text-align: center; }
    .content { background-color: #f9fafb; padding: 20px; margin: 20px 0; }
    .status-box { background-color: white; padding: 15px; border-left: 4px solid #8b5cf6; margin: 15px 0; }
    .detail-row { margin: 10px 0; }
    .label { font-weight: bold; color: #6b7280; }
    .value { color: #111827; }
    .footer { text-align: center; color: #6b7280; font-size: 12px; margin-top: 20px; }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h1>Task Status Confirmation</h1>
    </div>
    
    <div class="content">
      <p>Hello <%= @messenger.name %>,</p>
      <p>The task status has been successfully updated:</p>
      
      <div class="status-box">
        <div class="detail-row">
          <span class="label">Tracking Number:</span>
          <span class="value"><%= @task.barcode %></span>
        </div>
        <div class="detail-row">
          <span class="label">Previous Status:</span>
          <span class="value"><%= @old_status.titleize %></span>
        </div>
        <div class="detail-row">
          <span class="label">New Status:</span>
          <span class="value"><strong><%= @new_status.titleize %></strong></span>
        </div>
      </div>
      
      <% if @new_status == 'delivered' %>
        <p>✅ Great job! The task has been marked as delivered.</p>
      <% elsif @new_status == 'picked_up' %>
        <p>📦 Please proceed to the delivery location.</p>
      <% else %>
        <p>Continue to update the status as the delivery progresses.</p>
      <% end %>
    </div>
    
    <div class="footer">
      <p>FastePost Delivery System</p>
    </div>
  </div>
</body>
</html>
