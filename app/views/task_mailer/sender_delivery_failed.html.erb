<!DOCTYPE html>
<html>
<head>
  <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  <style>
    body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
    .container { max-width: 600px; margin: 0 auto; padding: 20px; }
    .header { background-color: #ef4444; color: white; padding: 20px; text-align: center; }
    .content { background-color: #f9fafb; padding: 20px; margin: 20px 0; }
    .failure-box { background-color: white; padding: 20px; border-left: 4px solid #ef4444; }
    .detail-row { margin: 10px 0; }
    .label { font-weight: bold; color: #6b7280; }
    .value { color: #111827; }
    .reason-box { background-color: #fee2e2; padding: 15px; margin: 15px 0; border-radius: 4px; }
    .footer { text-align: center; color: #6b7280; font-size: 12px; margin-top: 20px; }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h1>⚠️ Delivery Failed</h1>
    </div>
    
    <div class="content">
      <p>Hello <%= @sender.name %>,</p>
      <p>We were unable to complete the delivery of your shipment.</p>
      
      <div class="failure-box">
        <div class="detail-row">
          <span class="label">Tracking Number:</span>
          <span class="value"><%= @task.barcode %></span>
        </div>
        <div class="detail-row">
          <span class="label">Recipient:</span>
          <span class="value"><%= @task.customer.full_name %></span>
        </div>
        <div class="detail-row">
          <span class="label">Attempted Delivery:</span>
          <span class="value"><%= Time.current.strftime("%B %d, %Y at %I:%M %p") %></span>
        </div>
      </div>
      
      <div class="reason-box">
        <strong>Reason:</strong><br>
        <%= @failure_reason || 'Unable to complete delivery' %>
      </div>
      
      <p>Our team is working to reschedule the delivery. The recipient will be contacted shortly.</p>
    </div>
    
    <div class="footer">
      <p>FastePost Delivery System</p>
    </div>
  </div>
</body>
</html>
