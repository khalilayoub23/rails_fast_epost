<!DOCTYPE html>
<html>
<head>
  <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  <style>
    body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
    .container { max-width: 600px; margin: 0 auto; padding: 20px; }
    .header { background-color: #10b981; color: white; padding: 20px; text-align: center; }
    .success-icon { font-size: 48px; margin: 10px 0; }
    .content { background-color: #f9fafb; padding: 20px; margin: 20px 0; }
    .delivery-details { background-color: white; padding: 20px; border-left: 4px solid #10b981; }
    .detail-row { margin: 10px 0; }
    .label { font-weight: bold; color: #6b7280; }
    .value { color: #111827; }
    .button { background-color: #10b981; color: white; padding: 12px 24px; text-decoration: none; display: inline-block; margin: 20px 0; border-radius: 4px; }
    .footer { text-align: center; color: #6b7280; font-size: 12px; margin-top: 20px; }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <div class="success-icon">✅</div>
      <h1>Package Delivered!</h1>
    </div>
    
    <div class="content">
      <p>Hello <%= @customer.full_name %>,</p>
      <p><strong>Great news! Your package has been successfully delivered.</strong></p>
      
      <div class="delivery-details">
        <div class="detail-row">
          <span class="label">Tracking Number:</span>
          <span class="value"><%= @task.barcode %></span>
        </div>
        <div class="detail-row">
          <span class="label">Delivered By:</span>
          <span class="value"><%= @messenger.name %></span>
        </div>
        <div class="detail-row">
          <span class="label">Delivery Time:</span>
          <span class="value"><%= Time.current.strftime("%B %d, %Y at %I:%M %p") %></span>
        </div>
        <div class="detail-row">
          <span class="label">Delivery Location:</span>
          <span class="value"><%= @task.drop_off_location %></span>
        </div>
        <% if @task.recipient_name.present? %>
        <div class="detail-row">
          <span class="label">Received By:</span>
          <span class="value"><%= @task.recipient_name %></span>
        </div>
        <% end %>
        <% if @task.signature_image.present? %>
        <div class="detail-row">
          <span class="label">Signature:</span>
          <span class="value">✓ Captured</span>
        </div>
        <% end %>
      </div>
      
      <p>Thank you for choosing FastePost! We hope you're satisfied with our service.</p>
      
      <center>
        <a href="<%= root_url %>?tracking=<%= @task.barcode %>" class="button">View Delivery Details</a>
      </center>
      
      <p style="text-align: center; margin-top: 30px;">
        <small>If you have any questions or concerns about your delivery, please contact us.</small>
      </p>
    </div>
    
    <div class="footer">
      <p>FastePost Delivery System</p>
      <p>Thank you for your business!</p>
    </div>
  </div>
</body>
</html>
