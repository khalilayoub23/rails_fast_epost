<!DOCTYPE html>
<html>
<head>
  <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  <style>
    body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
    .container { max-width: 600px; margin: 0 auto; padding: 20px; }
    .header { background-color: #10b981; color: white; padding: 20px; text-align: center; }
    .success-icon { font-size: 48px; margin: 10px 0; }
    .content { background-color: #f9fafb; padding: 20px; margin: 20px 0; }
    .delivery-box { background-color: white; padding: 20px; border-left: 4px solid #10b981; }
    .detail-row { margin: 10px 0; }
    .label { font-weight: bold; color: #6b7280; }
    .value { color: #111827; }
    .footer { text-align: center; color: #6b7280; font-size: 12px; margin-top: 20px; }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <div class="success-icon">✅</div>
      <h1>Delivery Confirmation</h1>
    </div>
    
    <div class="content">
      <p>Hello <%= @sender.name %>,</p>
      <p><strong>Your shipment has been successfully delivered to the recipient.</strong></p>
      
      <div class="delivery-box">
        <div class="detail-row">
          <span class="label">Tracking Number:</span>
          <span class="value"><%= @task.barcode %></span>
        </div>
        <div class="detail-row">
          <span class="label">Recipient:</span>
          <span class="value"><%= @customer.full_name %></span>
        </div>
        <div class="detail-row">
          <span class="label">Delivery Time:</span>
          <span class="value"><%= Time.current.strftime("%B %d, %Y at %I:%M %p") %></span>
        </div>
        <% if @task.recipient_name.present? %>
        <div class="detail-row">
          <span class="label">Received By:</span>
          <span class="value"><%= @task.recipient_name %></span>
        </div>
        <% end %>
      </div>
      
      <p>Thank you for using FastePost for your delivery needs!</p>
    </div>
    
    <div class="footer">
      <p>FastePost Delivery System</p>
    </div>
  </div>
</body>
</html>
