<!DOCTYPE html>
<html>
<head>
  <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  <style>
    body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
    .container { max-width: 600px; margin: 0 auto; padding: 20px; }
    .header { background-color: #10b981; color: white; padding: 20px; text-align: center; }
    .content { background-color: #f9fafb; padding: 20px; margin: 20px 0; }
    .status-update { background-color: white; padding: 20px; border-left: 4px solid #10b981; margin: 20px 0; }
    .status-flow { display: flex; align-items: center; justify-content: center; margin: 20px 0; }
    .status-old { color: #6b7280; text-decoration: line-through; }
    .status-arrow { margin: 0 10px; color: #10b981; font-size: 24px; }
    .status-new { color: #10b981; font-weight: bold; font-size: 18px; }
    .detail-row { margin: 10px 0; }
    .label { font-weight: bold; color: #6b7280; }
    .value { color: #111827; }
    .button { background-color: #10b981; color: white; padding: 12px 24px; text-decoration: none; display: inline-block; margin: 20px 0; border-radius: 4px; }
    .footer { text-align: center; color: #6b7280; font-size: 12px; margin-top: 20px; }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h1>Shipment Status Update</h1>
    </div>
    
    <div class="content">
      <p>Hello <%= @customer.full_name %>,</p>
      <p>Your shipment status has been updated:</p>
      
      <div class="status-update">
        <div class="status-flow">
          <span class="status-old"><%= @old_status.titleize %></span>
          <span class="status-arrow">→</span>
          <span class="status-new"><%= @new_status.titleize %></span>
        </div>
        
        <div class="detail-row">
          <span class="label">Tracking Number:</span>
          <span class="value"><%= @task.barcode %></span>
        </div>
        <% if @task.messenger.present? %>
        <div class="detail-row">
          <span class="label">Messenger:</span>
          <span class="value"><%= @task.messenger.name %></span>
        </div>
        <% end %>
        <div class="detail-row">
          <span class="label">Updated:</span>
          <span class="value"><%= Time.current.strftime("%B %d, %Y at %I:%M %p") %></span>
        </div>
      </div>
      
      <% case @new_status %>
      <% when 'picked_up' %>
        <p>✅ Your package has been picked up and is on its way to the destination.</p>
      <% when 'in_transit' %>
        <p>🚚 Your package is currently in transit.</p>
      <% when 'out_for_delivery' %>
        <p>📦 Your package is out for delivery and will arrive soon!</p>
      <% when 'delivered' %>
        <p>🎉 Your package has been successfully delivered!</p>
      <% when 'failed' %>
        <p>⚠️ There was an issue with your delivery. Our team will contact you shortly.</p>
      <% else %>
        <p>Your shipment status has been updated.</p>
      <% end %>
      
      <center>
        <a href="<%= root_url %>?tracking=<%= @task.barcode %>" class="button">Track Shipment</a>
      </center>
    </div>
    
    <div class="footer">
      <p>FastePost Delivery System</p>
      <p>Need help? Contact us at support@fastepost.com</p>
    </div>
  </div>
</body>
</html>
