<div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
  <!-- Header -->
  <div class="mb-8">
    <div class="flex items-center justify-between">
      <div>
        <h1 class="text-3xl font-bold text-gray-900"><%= @document_template.name %></h1>
        <p class="mt-2 text-sm text-gray-600">
          <%= @document_template.description %>
        </p>
      </div>
      <%= link_to "Back to Template", admin_document_template_path(@document_template), class: "inline-flex items-center px-4 py-2 border border-gray-300 rounded-md shadow-sm text-sm font-medium text-gray-700 bg-white hover:bg-gray-50" %>
    </div>
  </div>

  <!-- Template Info -->
  <div class="bg-blue-50 border-l-4 border-blue-400 p-4 mb-6">
    <div class="flex">
      <div class="flex-shrink-0">
        <svg class="h-5 w-5 text-blue-400" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor">
          <path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2v-3a1 1 0 00-1-1H9z" clip-rule="evenodd" />
        </svg>
      </div>
      <div class="ml-3">
        <h3 class="text-sm font-medium text-blue-800">Fill in the required information</h3>
        <div class="mt-2 text-sm text-blue-700">
          <p>Complete all required fields marked with an asterisk (*) to generate your document.</p>
        </div>
      </div>
    </div>
  </div>

  <!-- Generation Form -->
  <%= form_with url: generate_admin_document_template_path(@document_template), method: :post, local: true, class: "space-y-6" do |form| %>
    
    <!-- Variable Fields -->
    <div class="bg-white shadow rounded-lg p-6">
      <h2 class="text-lg font-medium text-gray-900 mb-6">Document Variables</h2>
      
      <% if @document_template.variables.present? %>
        <div class="space-y-6">
          <% @document_template.variables.each do |var_name, var_config| %>
            <div>
              <%= label_tag "variables[#{var_name}]", var_config["label"] || var_name.titleize, class: "block text-sm font-medium text-gray-700" do %>
                <%= var_config["label"] || var_name.titleize %>
                <% if var_config["required"] %>
                  <span class="text-red-500">*</span>
                <% end %>
              <% end %>
              
              <% case var_config["type"] %>
              <% when "text", "textarea" %>
                <%= text_area_tag "variables[#{var_name}]", nil, 
                  rows: 3,
                  required: var_config["required"],
                  placeholder: "Enter #{var_config["label"] || var_name.titleize.downcase}",
                  class: "mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-blue-500 focus:ring-blue-500 sm:text-sm" %>
              <% when "date" %>
                <%= date_field_tag "variables[#{var_name}]", nil,
                  required: var_config["required"],
                  class: "mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-blue-500 focus:ring-blue-500 sm:text-sm" %>
              <% when "number" %>
                <%= number_field_tag "variables[#{var_name}]", nil,
                  required: var_config["required"],
                  step: "0.01",
                  placeholder: "Enter #{var_config["label"] || var_name.titleize.downcase}",
                  class: "mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-blue-500 focus:ring-blue-500 sm:text-sm" %>
              <% else %>
                <%= text_field_tag "variables[#{var_name}]", nil,
                  required: var_config["required"],
                  placeholder: "Enter #{var_config["label"] || var_name.titleize.downcase}",
                  class: "mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-blue-500 focus:ring-blue-500 sm:text-sm" %>
              <% end %>
              
              <% if var_config["description"] %>
                <p class="mt-1 text-sm text-gray-500"><%= var_config["description"] %></p>
              <% end %>
            </div>
          <% end %>
        </div>
      <% else %>
        <div class="text-center py-8">
          <svg class="mx-auto h-12 w-12 text-gray-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" />
          </svg>
          <h3 class="mt-2 text-sm font-medium text-gray-900">No variables defined</h3>
          <p class="mt-1 text-sm text-gray-500">This template doesn't require any variable input.</p>
        </div>
      <% end %>
    </div>

    <!-- Output Options -->
    <div class="bg-white shadow rounded-lg p-6">
      <h2 class="text-lg font-medium text-gray-900 mb-4">Output Options</h2>
      
      <div class="space-y-4">
        <div>
          <%= label_tag :filename, "Filename (optional)", class: "block text-sm font-medium text-gray-700" %>
          <%= text_field_tag :filename, nil,
            placeholder: "e.g., power-of-attorney-john-doe",
            class: "mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-blue-500 focus:ring-blue-500 sm:text-sm" %>
          <p class="mt-1 text-sm text-gray-500">Leave blank to use the template name. Extension will be added automatically.</p>
        </div>
      </div>
    </div>

    <!-- Action Buttons -->
    <div class="flex items-center justify-between space-x-4">
      <%= link_to "Cancel", admin_document_template_path(@document_template), 
        class: "inline-flex items-center px-6 py-3 border border-gray-300 rounded-md shadow-sm text-base font-medium text-gray-700 bg-white hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500" %>
      
      <div class="flex space-x-4">
        <%= button_tag type: "submit", name: "action", value: "preview",
          class: "inline-flex items-center px-6 py-3 border border-blue-600 rounded-md shadow-sm text-base font-medium text-blue-600 bg-white hover:bg-blue-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500" do %>
          <svg class="-ml-1 mr-2 h-5 w-5" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor">
            <path d="M10 12a2 2 0 100-4 2 2 0 000 4z" />
            <path fill-rule="evenodd" d="M.458 10C1.732 5.943 5.522 3 10 3s8.268 2.943 9.542 7c-1.274 4.057-5.064 7-9.542 7S1.732 14.057.458 10zM14 10a4 4 0 11-8 0 4 4 0 018 0z" clip-rule="evenodd" />
          </svg>
          Preview PDF
        <% end %>
        
        <%= button_tag type: "submit", name: "action", value: "download",
          class: "inline-flex items-center px-6 py-3 border border-transparent rounded-md shadow-sm text-base font-medium text-white bg-blue-600 hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500" do %>
          <svg class="-ml-1 mr-2 h-5 w-5" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor">
            <path fill-rule="evenodd" d="M3 17a1 1 0 011-1h12a1 1 0 110 2H4a1 1 0 01-1-1zm3.293-7.707a1 1 0 011.414 0L9 10.586V3a1 1 0 112 0v7.586l1.293-1.293a1 1 0 111.414 1.414l-3 3a1 1 0 01-1.414 0l-3-3a1 1 0 010-1.414z" clip-rule="evenodd" />
          </svg>
          Generate & Download
        <% end %>
      </div>
    </div>
  <% end %>

  <!-- Template Content Preview -->
  <% if @document_template.content.present? %>
    <div class="mt-8 bg-white shadow rounded-lg p-6">
      <h2 class="text-lg font-medium text-gray-900 mb-4">Template Preview</h2>
      <div class="prose prose-sm max-w-none">
        <div class="bg-gray-50 rounded-md p-4 font-mono text-xs overflow-x-auto">
          <%= simple_format(@document_template.content) %>
        </div>
      </div>
      <p class="mt-2 text-xs text-gray-500">
        Variables in the format {{variable_name}} will be replaced with your input values.
      </p>
    </div>
  <% end %>
</div>
