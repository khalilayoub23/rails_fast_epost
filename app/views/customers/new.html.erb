<% content_for :page_title, t("customers.new_title", default: "New Customer") %>

<div class="min-h-screen bg-gradient-to-br from-slate-950 via-slate-900 to-black py-10">
  <div class="container mx-auto max-w-4xl px-4">
    <div class="mb-6">
      <%= link_to customers_path, class: "inline-flex items-center text-sm font-semibold text-slate-300 hover:text-yellow-400" do %>
        <span class="material-icons mr-2 text-base">arrow_back</span>
        <%= t("common.back_to_list", default: "Back to customers") %>
      <% end %>
    </div>

    <div class="rounded-3xl border border-white/10 bg-white/5 shadow-2xl shadow-black/30 backdrop-blur">
      <div class="border-b border-white/10 px-6 py-5">
        <h1 class="text-2xl font-bold text-white"><%= t("customers.new_heading", default: "Create customer") %></h1>
        <p class="mt-2 text-sm text-slate-300"><%= t("customers.new_subheading", default: "Capture sender, business, or court contacts for deliveries.") %></p>
      </div>
      <div class="px-6 py-8">
        <%= render "form", customer: @customer %>
      </div>
    </div>
  </div>
</div>
