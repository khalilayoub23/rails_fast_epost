<aside id="sidebar" class="absolute left-0 top-0 z-9999 flex h-screen w-72.5 flex-col overflow-y-hidden bg-white dark:bg-boxdark border-r border-stroke dark:border-strokedark duration-300 ease-linear lg:static lg:translate-x-0 -translate-x-full">
  <!-- SIDEBAR HEADER -->
  <div class="flex items-center justify-between gap-2 px-4 py-6 lg:py-7">
    <%= render 'shared/logo', size: 'md', text: true %>
  </div>
  <!-- SIDEBAR HEADER -->

  <div class="no-scrollbar flex flex-col overflow-y-auto duration-300 ease-linear">
    <!-- Sidebar Menu -->
    <nav class="mt-5 py-4 px-4 lg:mt-9 lg:px-6">
      <div>
        <ul class="mb-6 flex flex-col gap-1.5">
          <li>
            <%= link_to root_path, class: "group relative flex items-center gap-2.5 rounded-md py-2 px-4 font-medium text-body duration-300 ease-in-out hover:bg-whiter #{'bg-whiter text-primary' if current_page?(root_path)}" do %>
              <span class="material-icons text-lg #{'text-primary' if current_page?(root_path)}">space_dashboard</span>
              Dashboard
            <% end %>
          </li>
          <% if current_user&.manager? %>
          <li>
            <%= link_to payments_path, class: "group relative flex items-center gap-2.5 rounded-md py-2 px-4 font-medium text-body duration-300 ease-in-out hover:bg-whiter #{'bg-whiter text-primary' if current_page?(payments_path)}" do %>
              <span class="material-icons text-lg #{'text-primary' if current_page?(payments_path)}">payment</span>
              Payments
            <% end %>
          </li>
          <% end %>
          <li>
            <%= link_to customers_path, class: "group relative flex items-center gap-2.5 rounded-md py-2 px-4 font-medium text-body duration-300 ease-in-out hover:bg-whiter #{'bg-whiter text-primary' if current_page?(customers_path)}" do %>
              <span class="material-icons text-lg #{'text-primary' if current_page?(customers_path)}">people</span>
              Customers
            <% end %>
          </li>
          <li>
            <%= link_to tasks_path, class: "group relative flex items-center gap-2.5 rounded-md py-2 px-4 font-medium text-body duration-300 ease-in-out hover:bg-whiter #{'bg-whiter text-primary' if current_page?(tasks_path)}" do %>
              <span class="material-icons text-lg #{'text-primary' if current_page?(tasks_path)}">task</span>
              Tasks
            <% end %>
          </li>
          <% if current_user&.admin? %>
          <li>
            <%= link_to carriers_path, class: "group relative flex items-center gap-2.5 rounded-md py-2 px-4 font-medium text-body duration-300 ease-in-out hover:bg-whiter #{'bg-whiter text-primary' if current_page?(carriers_path)}" do %>
              <span class="material-icons text-lg #{'text-primary' if current_page?(carriers_path)}">local_shipping</span>
              Carriers
            <% end %>
          </li>
          <li>
            <%= link_to admin_senders_path, class: "group relative flex items-center gap-2.5 rounded-md py-2 px-4 font-medium text-body duration-300 ease-in-out hover:bg-whiter #{'bg-whiter text-primary' if current_page?(admin_senders_path)}" do %>
              <span class="material-icons text-lg #{'text-primary' if current_page?(admin_senders_path)}">send</span>
              Senders
            <% end %>
          </li>
          <li>
            <%= link_to admin_messengers_path, class: "group relative flex items-center gap-2.5 rounded-md py-2 px-4 font-medium text-body duration-300 ease-in-out hover:bg-whiter #{'bg-whiter text-primary' if current_page?(admin_messengers_path)}" do %>
              <span class="material-icons text-lg #{'text-primary' if current_page?(admin_messengers_path)}">pedal_bike</span>
              Messengers
            <% end %>
          </li>
          <li>
            <%= link_to admin_lawyers_path, class: "group relative flex items-center gap-2.5 rounded-md py-2 px-4 font-medium text-body duration-300 ease-in-out hover:bg-whiter #{'bg-whiter text-primary' if current_page?(admin_lawyers_path)}" do %>
              <span class="material-icons text-lg #{'text-primary' if current_page?(admin_lawyers_path)}">gavel</span>
              Lawyers
            <% end %>
          </li>
          <li>
            <%= link_to admin_document_templates_path, class: "group relative flex items-center gap-2.5 rounded-md py-2 px-4 font-medium text-body duration-300 ease-in-out hover:bg-whiter #{'bg-whiter text-primary' if current_page?(admin_document_templates_path)}" do %>
              <span class="material-icons text-lg #{'text-primary' if current_page?(admin_document_templates_path)}">description</span>
              Document Templates
            <% end %>
          </li>
          <li>
            <%= link_to admin_contact_inquiries_path, class: "group relative flex items-center gap-2.5 rounded-md py-2 px-4 font-medium text-body duration-300 ease-in-out hover:bg-whiter #{'bg-whiter text-primary' if current_page?(admin_contact_inquiries_path)}" do %>
              <span class="material-icons text-lg #{'text-primary' if current_page?(admin_contact_inquiries_path)}">mail</span>
              Contact Inquiries
              <% if @pending_count ||= ContactInquiry.pending.count > 0 %>
                <span class="ml-auto bg-warning text-white rounded-full px-2 py-0.5 text-xs"><%= @pending_count %></span>
              <% end %>
            <% end %>
          </li>
          <li>
            <%= link_to admin_branding_path, class: "group relative flex items-center gap-2.5 rounded-md py-2 px-4 font-medium text-body duration-300 ease-in-out hover:bg-whiter #{'bg-whiter text-primary' if current_page?(admin_branding_path)}" do %>
              <span class="material-icons text-lg #{'text-primary' if current_page?(admin_branding_path)}">brush</span>
              Branding
            <% end %>
          </li>
          <li>
            <%= link_to admin_dashboard_path, class: "group relative flex items-center gap-2.5 rounded-md py-2 px-4 font-medium text-body duration-300 ease-in-out hover:bg-whiter #{'bg-whiter text-primary' if current_page?(admin_dashboard_path)}" do %>
              <span class="material-icons text-lg #{'text-primary' if current_page?(admin_dashboard_path)}">admin_panel_settings</span>
              Admin Dashboard
            <% end %>
          </li>
          <li>
            <%= link_to new_admin_pdfs_path, class: "group relative flex items-center gap-2.5 rounded-md py-2 px-4 font-medium text-body duration-300 ease-in-out hover:bg-whiter #{'bg-whiter text-primary' if current_page?(new_admin_pdfs_path)}" do %>
              <span class="material-icons text-lg #{'text-primary' if current_page?(new_admin_pdfs_path)}">picture_as_pdf</span>
              PDF Tools
            <% end %>
          </li>
          <% end %>
          <li class="mt-2 border-t border-stroke"></li>
          <li>
            <%= link_to profile_path, class: "group relative flex items-center gap-2.5 rounded-md py-2 px-4 font-medium text-body duration-300 ease-in-out hover:bg-whiter #{'bg-whiter text-primary' if current_page?(profile_path)}" do %>
              <span class="material-icons text-lg #{'text-primary' if current_page?(profile_path)}">person</span>
              Profile
            <% end %>
          </li>
          <li>
            <%= link_to settings_path, class: "group relative flex items-center gap-2.5 rounded-md py-2 px-4 font-medium text-body duration-300 ease-in-out hover:bg-whiter #{'bg-whiter text-primary' if current_page?(settings_path)}" do %>
              <span class="material-icons text-lg #{'text-primary' if current_page?(settings_path)}">settings</span>
              Settings
            <% end %>
          </li>
        </ul>
      </div>
    </nav>
    <!-- Sidebar Menu -->
  </div>
</aside>
