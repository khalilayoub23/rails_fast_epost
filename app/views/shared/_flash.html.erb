<% flash.each do |k, v| %>
  <div data-controller="flash"
       class="mb-4 rounded px-4 py-3 <%= k.to_s == "alert" ? "bg-red-50 text-red-700" : "bg-green-50 text-green-700" %>">
    <%= v %>
  </div>
<% end %>
