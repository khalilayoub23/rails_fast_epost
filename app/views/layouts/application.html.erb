<!DOCTYPE html>
<html lang="<%= I18n.locale %>" dir="<%= text_direction %>">
  <head>
    <title><%= content_for(:title) || "Fast Epost 3" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1,maximum-scale=1,user-scalable=no,viewport-fit=cover">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
    <meta name="mobile-web-app-capable" content="yes">
    <meta name="theme-color" content="#1C2434">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    
    <%# Turbo Native bridge %>
    <meta name="turbo-visit-control" content="reload">

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

  <%# Load Tailwind first, then fonts, then conditionally RTL support, then our application overrides and icons (order matters) %>
  <% unless Rails.env.test? %>
    <% if rtl? %>
      <%= stylesheet_link_tag "tailwind", "fonts", "rtl", "application", "dashboard-theme", "material-icons", "data-turbo-track": "reload" %>
    <% else %>
      <%= stylesheet_link_tag "tailwind", "fonts", "application", "dashboard-theme", "material-icons", "data-turbo-track": "reload" %>
    <% end %>
    <%= javascript_importmap_tags %>
  <% end %>
    <meta name="color-scheme" content="light dark">
    <script>
      // Initialize dark mode and RTL early to avoid flash of wrong theme
      (function() {
        try {
          var ls = localStorage.getItem('ui.dark');
          var dark = (ls === 'true') || (ls === null && window.matchMedia && window.matchMedia('(prefers-color-scheme: dark)').matches);
          var dir = localStorage.getItem('ui.direction') || 'ltr';
          document.documentElement.classList.toggle('dark', dark);
          document.documentElement.setAttribute('dir', dir);
        } catch (e) {}
      })();
    </script>
  </head>

  <body class="bg-gradient-to-br from-gray-900 via-gray-800 to-black font-satoshi text-base text-gray-200">
    <div class="flex h-screen overflow-hidden" data-controller="sidebar">
      <!-- Hide sidebar on mobile/Turbo Native -->
      <% unless turbo_native_app? %>
        <%= render "shared/sidebar" %>
      <% end %>
      
      <div class="relative flex flex-1 flex-col overflow-y-auto overflow-x-hidden bg-transparent <%= 'pb-20' if turbo_native_app? %>">
        <!-- Hide topbar on Turbo Native (native apps have their own nav) -->
        <% unless turbo_native_app? %>
          <%= render "shared/topbar" %>
        <% end %>
        
        <main class="mx-auto max-w-screen-2xl p-4 md:p-6 2xl:p-10">
          <%= render "shared/flash_messages" %>
          <%= yield %>
        </main>
      </div>
    </div>
    
    <!-- Mobile Bottom Navigation (Turbo Native only) -->
    <%= render "shared/mobile_bottom_nav" %>
  </body>
</html>
