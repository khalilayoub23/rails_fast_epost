<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Fast Epost 3" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

  <%# Load Tailwind first, then fonts, then our application overrides and icons (order matters) %>
  <%= stylesheet_link_tag "tailwind", "fonts", "application", "material-icons", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <meta name="color-scheme" content="light dark">
    <script>
      // Initialize dark mode and RTL early to avoid flash of wrong theme
      (function() {
        try {
          var ls = localStorage.getItem('ui.dark');
          var dark = (ls === 'true') || (ls === null && window.matchMedia && window.matchMedia('(prefers-color-scheme: dark)').matches);
          var dir = localStorage.getItem('ui.direction') || 'ltr';
          document.documentElement.classList.toggle('dark', dark);
          document.documentElement.setAttribute('dir', dir);
        } catch (e) {}
      })();
    </script>
  </head>

  <body class="bg-whiten font-satoshi text-base text-body dark:bg-boxdark-2 dark:text-bodydark">
    <div class="flex h-screen overflow-hidden" data-controller="sidebar">
      <%= render "shared/sidebar" %>
      <div class="relative flex flex-1 flex-col overflow-y-auto overflow-x-hidden bg-whiten dark:bg-boxdark-2">
        <%= render "shared/topbar" %>
        <main class="mx-auto max-w-screen-2xl p-4 md:p-6 2xl:p-10 bg-whiten dark:bg-boxdark-2">
          <%= render "shared/flash" %>
          <%= yield %>
        </main>
      </div>
    </div>
  </body>
</html>
