<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <title><%= content_for(:title) || "Fast Epost Logo Test" %></title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "logo_test", "data-turbo-track": "reload" %>
    <%= yield :stylesheets %>
  </head>
  <body class="<%= content_for?(:body_class) ? yield(:body_class) : "logo-test-body" %>">
    <%= yield %>
  </body>
</html>
